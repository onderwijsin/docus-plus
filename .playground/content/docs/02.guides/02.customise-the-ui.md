---
title: Customise the UI
description: Adapt the shared documentation shell to your project.
---

The layer supplies sensible defaults, but the consuming project owns its visual identity.

## Add app configuration

Use `app/app.config.ts` for Docus and Nuxt UI settings such as the header title, colors, GitHub
metadata, and assistant behavior.

```ts
export default defineAppConfig({
  header: { title: "Example Docs" },
  ui: {
    colors: {
      primary: "indigo",
      neutral: "slate"
    }
  }
});
```

## Keep the layer stylesheet

Your `app/app.css` must import the shared stylesheet before adding project-specific tokens:

```css
@import "#layers/docus-plus/app/app.css";

@theme {
  --color-brand-500: oklch(60% 0.2 250);
}
```

You can also replace `AppHeaderLogo` from `app/components/AppHeaderLogo.vue` when the default logo
does not represent your project.

::tip{title="Start with the consumer boundary"}
Keep content and branding in the consuming application. Change the layer only when the behavior is
useful to every site built on it.
::
