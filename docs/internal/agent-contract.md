# Agent contract

The authoritative instructions for coding agents are in the repository root
[`AGENTS.md`](../../AGENTS.md). This page is intentionally a pointer so the operational contract does
not drift from the root instructions.
