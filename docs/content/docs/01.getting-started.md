---
title: Getting Started
description: Create a small documentation site with the Docus Plus layer.
navigation:
  icon: lucide:rocket
---

The official Docus Plus site is a consumer application built on top of the layer. The layer provides the
documentation shell and reusable integrations; the consuming application supplies its content,
identity, visual tokens, and brand components.

This page walks through the setup from an empty Nuxt project to a working documentation site.

## 1. Install and extend the layer

Install the published layer with the project-pinned package manager:

```bash
corepack pnpm add @onderwijsin/docus-plus
```

Extend it from the consuming application's `nuxt.config.ts`:

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  extends: ["@onderwijsin/docus-plus#v1.0.0"]
});
```

Pin the layer version in production so that a new layer release does not change the site without a
deliberate update.

For a complete starting point, copy `.starters/default` from the Docus Plus repository. It has the
same package-based extension and includes working examples for the available integrations.

## 2. Add the environment

Create the environment values required by the layer. At minimum, configure the site URL and the
environment mode:

```dotenv [.env]
APP_ENV=development
MODE=development
NUXT_PUBLIC_SITE_URL=http://localhost:3000
```

The README lists the complete environment contract. Integrations such as Mistral and Cloudflare add
their own conditional values.

For a validated profile setup with Proton Pass, continue with
[Manage environment variables with Varlock](/guides/varlock-and-proton-pass).

## 3. Add the landing page

Create `content/index.yml`. The layer validates the landing-page shape with Nuxt Content, so include
SEO metadata, a hero, features, and a call to action:

```yml [content/index.yml]
seo:
  title: Example documentation
  description: Documentation for the Example project.
  ogImage: https://example.com/og-image.png
hero:
  title: Build with the Example platform
  description: Learn how to use the Example platform.
  links:
    - label: Get started
      to: /getting-started
      color: primary
features:
  title: Everything you need
  description: A focused set of guides and references.
  items:
    - icon: i-lucide-book-open
      title: Guides
      description: Explain the main integration paths.
cta:
  title: Start building
  description: Follow the guides to create your first integration.
  links:
    - label: Read the guide
      to: /getting-started
      color: primary
```

The starter's landing page is a complete working version of this example.

## 4. Add documentation pages

Create Markdown files below `content/docs/`. Nuxt Content turns each file into a route and Docus
uses its frontmatter to build page metadata and navigation:

```md [content/docs/01.getting-started.md]
---
title: Getting started
description: Create your first integration.
navigation:
  icon: lucide:rocket
---

Your guide starts here.
```

Use numbered filenames to control ordering. Add `.navigation.yml` to a directory when you want a
section title or icon.

## 5. Configure the consuming application

Keep consumer-specific settings in the consuming application. The layer's defaults are reusable,
while the app owns its identity, branding, and content:

```ts [app/app.config.ts]
export default defineAppConfig({
  header: { title: "Example Docs" },
  ui: {
    colors: { primary: "indigo", neutral: "slate" }
  }
});
```

Continue with the guides to configure content, the UI, integrations, and deployment.

::tip{title="Build on the starter"}
The starter's landing page, app configuration, stylesheet, and `AppHeaderLogo` override show a
complete consumer setup that you can adapt rather than recreate.
::

## Run the site

Start your documentation site locally:

```bash
corepack pnpm dev
```

For a static deployment with prerendered OG images, generate the site and deploy the public output:

```bash
corepack pnpm generate
```

The generated `.output/public/` directory contains the HTML, assets, and prerendered
OG images.

## Continue with the guides

- [Working with content](/guides/working-with-content) explains pages, navigation, and landing content.
- [Customise the UI](/guides/customise-the-ui) explains consumer branding and OG image templates.
- [Integrations and deployment](/guides/integrations-and-deployment) covers the assistant, API reference, and production output.
- [Manage environment variables with Varlock](/guides/varlock-and-proton-pass) covers validated profiles and Proton Pass secrets.
- [Scalar and OpenAPI reference](/guides/scalar-and-openapi) explains the automatic API Explorer and parsing.
