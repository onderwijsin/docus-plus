---
title: Environment
description: Use Varlock, the Vite integration, and Proton Pass to load validated secrets.
navigation:
  icon: lucide:key-round
---

The layer uses [Varlock](https://varlock.dev/) to validate environment variables and generate
TypeScript types. A recommended setup is to keep the environment contract in the repository and
load secret values from Proton Pass during local development and CI.

This guide assumes the consuming project has copied the `envs/` directory from the layer. Keep the
schema and profile files in the same directory configured by `package.json`:

```json [package.json]
{
  "varlock": {
    "loadPath": "./envs/"
  }
}
```

## Add the Vite integration

Install the integration in the consuming project:

```bash
corepack pnpm add -D varlock @varlock/vite-integration
```

Add the plugin to the consumer's Nuxt configuration:

```ts [nuxt.config.ts]
import { varlockVitePlugin } from "@varlock/vite-integration";

export default defineNuxtConfig({
  vite: {
    plugins: [varlockVitePlugin({ ssrInjectMode: "auto-load" })]
  }
});
```

`auto-load` makes the built server load the validated environment produced by Varlock. This is
useful for a Node server deployment where the runtime can access the profile files. Run Nuxt
through Varlock so the same validation happens in development, type generation, and production:

```bash
corepack pnpm varlock run -- nuxi dev
corepack pnpm varlock run -- nuxi build
```

This starter already contains the Vite plugin dependency and a commented configuration example in
`nuxt.config.ts`. Uncomment it when testing this integration locally.

For a deployment where the built artifact cannot read profile files at runtime, choose an
injection mode intentionally. Read the [Varlock Vite integration guide](https://varlock.dev/integrations/vite/)
before using a mode that embeds resolved secrets in the build output. For a non-static Cloudflare
Worker, use the platform-specific integration instead of bundling secrets with a plain Vite
plugin.

## Connect Proton Pass

Install and authenticate the official Proton Pass CLI, then add the Varlock plugin to the
consumer's schema. The plugin resolves `pass://` references through the CLI:

```bash
corepack pnpm add -D @varlock/proton-pass-plugin
```

Add this near the top of `envs/.env.schema`:

```dotenv [envs/.env.schema]
# @plugin(@varlock/proton-pass-plugin)
# @initProtonPass(personalAccessToken=$PROTON_PASS_PERSONAL_ACCESS_TOKEN)
```

Then map application variables to fields in a Proton Pass vault. Use the vault, item, and field
names from your own Proton Pass setup:

```dotenv [envs/.env.production]
API_TOKEN=protonPass(pass://Docus%20Plus/Production/API_TOKEN)
MISTRAL_API_KEY=protonPass(pass://Docus%20Plus/Production/MISTRAL_API_KEY)
```

Do not put Proton Pass credentials in the schema or in a tracked profile. For CI, the recommended
bootstrap variable is `PROTON_PASS_PERSONAL_ACCESS_TOKEN`. An already authenticated local CLI
session can be used for local work instead. The [Proton Pass plugin guide](https://varlock.dev/plugins/proton-pass/)
describes the alternative username, password, TOTP, and extra-password authentication options.

## Populate each environment profile

Varlock selects a profile from `APP_ENV`. With the repository's `loadPath`, the conventional files
are `envs/.env.development`, `envs/.env.preview`, `envs/.env.production`, and `envs/.env.next`.
Start with the values that apply to every profile:

```dotenv [envs/.env.development]
MODE=development
NITRO_PRESET=node-server
NUXT_PUBLIC_SITE_URL=http://localhost:3000
```

For preview or production, use the public HTTPS URL and select the deployment target explicitly:

```dotenv [envs/.env.production]
MODE=production
NITRO_PRESET=node-server
NUXT_PUBLIC_SITE_URL=https://docs.example.com
```

The required application values are:

| Variable                | Purpose                                                                   |
| ----------------------- | ------------------------------------------------------------------------- |
| `MODE`                  | Selects development, preview, production, or test behavior.               |
| `NUXT_PUBLIC_SITE_URL`  | Canonical URL used by SEO metadata, sitemaps, and absolute OG image URLs. |
| `API_TOKEN`             | Protects internal and admin-style routes.                                 |
| `MISTRAL_API_KEY`       | Enables the documentation assistant.                                      |
| `AI_GATEWAY_API_KEY`    | Required by the Docus AI module setup.                                    |

Add these conditionally:

- `OPENAPI_SOURCE_TYPE` and `OPENAPI_LOCATION` when API reference content is enabled.
- `MAILCHIMP_API_KEY`, `MAILCHIMP_LIST`, and `MAILCHIMP_SERVER` when newsletter signup is enabled.
- `CLOUDFLARE_WORKER_NAME`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_CACHE_NAMESPACE_ID`, and
  `CLOUDFLARE_KV_API_TOKEN` when `NITRO_PRESET=cloudflare_module`.
- `REDIS_*` variables when runtime caching uses Redis.

Use [Varlock environments](https://varlock.dev/guides/environments/) for profile selection and
the layer README for the complete variable contract. Keep secret values in Proton Pass or the CI
secret store; commit only the schema, safe profile defaults, and references.

## Verify the setup

Validate and generate the environment types before starting Nuxt:

```bash
corepack pnpm varlock load
corepack pnpm env:typegen
corepack pnpm varlock run -- nuxi dev
```

If a required value is missing, fix the selected profile or Proton Pass reference rather than
silencing the validation error. In CI, make sure `PROTON_PASS_PERSONAL_ACCESS_TOKEN` is available
before Varlock loads `envs/.env.schema`.

::tip{title="Keep provider wiring in the consuming project"}
The layer defines the environment contract, but the consuming project owns its Proton Pass vault
layout, profile values, CI credentials, and deployment-specific secrets.
::
